	
	fmt.Println("Produto criado")
	//Cria um novo servidor
	newProduct := product{
		id: 1,
		name: "arroz",
		price:	27,	
	}
	fmt.Println("Produto inserido")
	put(newProduct.id ,newProduct)
	fmt.Println("Produto recuperado")
	fmt.Println(get(newProduct.id).name)	
	fmt.Println("working")





    var lista int
    for _,nomeSala := range reflect.ValueOf(contacts).MapKeys() {
		lista = (nomeSala.Interface().(int))
        //fmt.Println(contacts[lista].Name)
        fmt.Println(contacts[lista].Number)
    }



		if(!reply){ 
		fmt.Printf("Not Working") 
	}else{
		fmt.Printf("Working") 
	}